# Runtime configuration is supplied by the VPS deployment. Do not point the
# application at a local PostgreSQL instance.
KAIHOU_VAULT_ROOT=/opt/kaihou-vault
DATABASE_URL=postgresql://kaihou_gm:${POSTGRES_PASSWORD}@postgres:5432/kaihou_gm
