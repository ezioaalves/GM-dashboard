# Copy to .env on the VPS deploy path, fill in real values, never commit.

POSTGRES_PASSWORD=change-me

# Path to the manual-pull clone of the Kaihou vault repo on the VPS
# (see DEPLOY.md — e.g. git clone git@github.com:ezioaalves/Kaihou.git /opt/kaihou-vault)
KAIHOU_VAULT_ROOT=/opt/kaihou-vault
